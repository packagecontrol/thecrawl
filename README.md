# the-registry

Canonical lifecycle registry for Package Control crawling.

- Primary artifact: `registry.json`
- Updated by CI from `thecrawl` workflow

This branch is used as seed/recovery input for `scripts.generate_registry`.
